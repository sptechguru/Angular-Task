import { NumericFormatterPipe } from './numeric-formatter.pipe';

describe('NumericFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new NumericFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
